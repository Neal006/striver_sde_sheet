1.
Problem : Set Matrix Zeroes         
PatternKey ~ Two-pass simulation 
Insight ~ 
- Can't modify while scanning ;
- Phase 1 observe only, Phase 2 modify only. 
- Store which rows/cols have zeros in O(m+n) space

2.
Problem : Pascal's Triangle
PatternKey ~ Simulation / build row by row
Insight ~ 
- Each middle element = prev[j-1] + prev[j]. 
- First and last always 1. 
- Build iteratively, no need for full DP table

3.
Problem : Next Permutation
PatternKey : pivot, swap, reverse
Insight : 
- Find rightmost ascent (pivot); 
- swap with smallest-larger in tail; 
- reverse tail.